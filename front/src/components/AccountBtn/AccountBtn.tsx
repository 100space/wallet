import { useGetMode } from "@hooks/useMode"
import { AccountBtnWrap, AccountBtnWrapper, AccountGetBtn } from "./styled"
import { useState } from "react";

export const AccountBtn = () => {
    // const [modeState, setChange] = useGetMode()
    // const [currentPage, setCurrentPage] = useState(null);
    
    // const AccountBtnList = [
    //     { content: "지갑 생성하기"},
    //     { content: "지갑 가져오기"},
    // ];

    // return(
    //     <>
    //         <AccountBtnWrapper >
    //             <AccountBtnWrap>
    //                 {AccountBtnList.map((btn, index) => (
    //                     // <AccountGetBtn key={index} mode={modeState.mode} onClick={handlePageChange}>{btn.content}</AccountGetBtn>
    //                 ))}
    //             </AccountBtnWrap>
    //         </AccountBtnWrapper>
    //     </>
    )
}